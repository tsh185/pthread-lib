# task 1
task

#every hour
interval=01:00:00

# what happens if a thread is not responding?
thread.action
  action=replace

# what do we do when the work load increases
work.load.increase
  bounds=10>
  action=add
  value=1

work.load.decrease
  bounds=2<
  action=sub
  value=1
